# Source global definitions
if [ -f /etc/bashrc ]; then
    . /etc/bashrc
fi

source .bashrc

(analysis-env) [aaportel@cmslpc124 ~]$ cat .bash_profile 
# Source global definitions
if [ -f /etc/bashrc ]; then
    . /etc/bashrc
fi

source .bashrc
